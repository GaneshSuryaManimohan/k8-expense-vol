apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql
  namespace: expense
spec:
  storageClassName: expense-ebs-sc # Reference the custom StorageClass
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 6Gi